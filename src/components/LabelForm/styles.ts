import styled from "styled-components";

export const Label = styled.label`
  color: var(--gray-0);
  font-family: var(--font-family);
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 16px;
`;
