import styled from "styled-components";

export const Title = styled.h1`
  font-family: var(--font-family);
  font-style: normal;
  font-weight: 700;
  font-size: 1.2rem;
  line-height: 22px;
  color: var(--gray-0);
`;
