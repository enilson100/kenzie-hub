import styled from "styled-components";

export const NoCardTitle = styled.h1`
  font-family: var(--font-family);
  font-style: normal;
  font-weight: 700;
  font-size: 18px;
  line-height: 24px;

  color: var(--gray-0);
`;
